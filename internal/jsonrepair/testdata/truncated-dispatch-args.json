{"agent": "coder", "task": "在 C:\\Users\\xzy\\Desktop\\gs\\pc-client 的「主机 Tab 左侧设备列表」中，**移除设备行右侧那个「云机数量 + 展开箭头」按钮**（方案 A：只删这个 UI 入口，保留其余第三层相关代码）。\n\n## 精确目标\n文件：`MYT/frontend/src/pages/host/components/DeviceListItem.tsx`（约 155 行）\n待删除：**行 94-105** 的那个按钮 —— 形如\n```tsx\n{onToggleExpand && (\n  <button\n    onClick={(e) => { e.stopPropagation(); onToggleExpand(device.device_id) }}\n    className={`flex items-center gap-0.5 text-[10px] px-1 py-0.5 rounded transition-colors shrink-0 ${...}`}\n    title={expanded ? t('host:deviceList.collapseAndroid') : t('host:deviceList.expandAndroid')}\n  >\n    {expanded ? <ChevronDown size={11} /> : <ChevronRight size={11} />}\n    <span>{androidCount ?? 0}</span>\n  </button>\n)}\n